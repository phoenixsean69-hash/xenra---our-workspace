# Sample Project

Use this folder to test XENRA 0.1.

- `hello.py` can be run with the Run button if Python is installed.
- `src/calculator.py` gives you a nested folder to test Explorer navigation.
- `src/main.cpp` can be edited now; compiler build profiles come in the next milestone.
